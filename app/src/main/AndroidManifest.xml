<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="technical.test.pokedex">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name="technical.test.pokedex.PokedexApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="technical.test.pokedex.ui.pokemondetail.view.PokemonDetailActivity"
                android:exported="false">
        </activity>
        <activity android:name="technical.test.pokedex.ui.pokemonhunter.view.PokemonHunterActivity"
                android:exported="false">
        </activity>
        <activity
                android:name="technical.test.pokedex.ui.MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>